<div class="col-sm-8 blog-main">
  <h1>New Post</h1>
  <if user_signed_in? %>
       <p><%= link_to "Post It", new_message_path, class: "navbar-text navbar-link" %></p>
  <%= render 'form', blog: @blog %>

  <%= link_to 'Back', blogs_path %>
</div>
